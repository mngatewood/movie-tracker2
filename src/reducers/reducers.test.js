import movieReducer from './movieReducer';

// describe('reducers', () => {
//   describe('movieReducer', () => {
//     it('should return the default state', () => {
//       expect(movieReducer(undefined, null)).toEqual([ ==])
//     })
//   })
// })